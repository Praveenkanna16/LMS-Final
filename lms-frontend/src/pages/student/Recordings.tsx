import React from 'react';
import StudentRecordedContent from './RecordedContent';

const StudentRecordings: React.FC = () => {
  // Reuse the existing RecordedContent component
  return <StudentRecordedContent />;
};

export default StudentRecordings;
